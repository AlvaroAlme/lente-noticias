# Copia este archivo como ".env" en desarrollo local.
# En Vite, solo las variables con prefijo VITE_ llegan al código del
# navegador (el resto se ignoran a propósito, por seguridad).
VITE_API_URL=http://localhost:4000
